# Desktop application
PyQt6
PyQt6-WebEngine

# Prediction engine
requests
biopython>=1.83

# RSA filter + ML scorer (Phase 1 & 2)
scikit-learn>=1.4
joblib>=1.3
xgboost>=2.0
# Optional: pure-Python DSSP fallback when mkdssp binary is unavailable
# pydssp>=0.9
